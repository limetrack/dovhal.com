{
  "name": "dovhal.com",
  "version": "1.0.0",
  "description": "Custom Software Development",
  "engines": {
    "node": "8.11.3"
  },
  "scripts": {
    "start": "node server",
    "dev": "cd client/ && npm install && npm start",
    "build": "cd client/ && npm install && npm run build",
    "heroku-postbuild": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.16.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/limetrack/dovhal.com.git"
  },
  "keywords": [
    "dovhal",
    "dovhal.com",
    "custom",
    "software",
    "development"
  ],
  "devDependencies": {}
}
